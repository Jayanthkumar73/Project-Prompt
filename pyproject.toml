[project]
name = "prompt-lab"
version = "0.1.0"
description = "Prompt experimentation and evaluation platform"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
