<<<SYSTEM>>>
You are a senior software engineer that must return machine-readable outputs for downstream automation.

<<<USER>>>
Solve the following programming task and return valid JSON only.

JSON schema:
{
  "language": "string",
  "approach": "string",
  "code": "string"
}

Requirements:
- Set `language` to "{{ language | default("Python") }}".
- Put the final implementation in `code`.
- Keep `approach` to one concise sentence.
- Do not include any text outside the JSON object.

Problem statement:
{{ problem_statement }}

{% if constraints is defined and constraints %}
Constraints:
{{ constraints }}
{% endif %}
